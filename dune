(library
  (name gmap)
  (public_name gmap)
  (modules gmap))

(test
  (name tests)
  (modules tests)
  (libraries alcotest fmt gmap))

(executable
  (name bench)
  (modules bench)
  (libraries mtime.clock.os ocplib-endian gmap fmt rresult))